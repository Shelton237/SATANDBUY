# Settings Service (scaffold)

Ce dossier prépare l'extraction du domaine **Settings**.

## TODO
- Déporter `Setting.js`, `settingController.js` et routes associées dans `packages/settings-domain`.
- Brancher une base Mongo dédiée + cache Redis.
- Exposer `GET /settings/store` pour la boutique.
